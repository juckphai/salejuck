rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /pos/data {
      // อนุญาตเฉพาะผู้ที่ Login ผ่านระบบ Firebase (แอปคุณใช้ Anonymous Login อยู่แล้ว)
      allow read, write: if request.auth != null;
    }
  }
}